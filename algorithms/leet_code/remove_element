class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        i , j = 0, len(nums) - 1

        while j > i:
            if nums[i] == val:
                temp = nums[j]
                nums[j] = val
                nums[i] = temp
                i += 1
                j -= 1
        
        print(nums)
